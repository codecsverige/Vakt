# 🚀 دليل بناء APK - الطريقة الدقيقة

## ⚡ الأمر الوحيد المطلوب (15-30 ثانية)

```bash
cd /workspace/FakturaVakt && export ANDROID_HOME=~/android-sdk && ./build-apk.sh
```

## 📍 مكان APK بعد البناء

```
/workspace/FakturaVakt/android/app/build/outputs/apk/release/app-release.apk
```

## 📦 رفع APK إلى GitHub

```bash
# استبدل v0.1.1 برقم الإصدار الجديد
gh release create v0.1.1 /workspace/FakturaVakt/android/app/build/outputs/apk/release/app-release.apk --title "v0.1.1" --notes "وصف التحديث"
```

## ⚠️ معلومات مهمة جداً

### ✅ هذه الطريقة تعمل لأن:
1. **البناء محلي** في `/workspace` وليس على GitHub Actions
2. **السكريبت** `build-apk.sh` موجود ويعمل
3. **Android SDK** مُثبت في `~/android-sdk`
4. **لا يحتاج دفع مال** - كل شيء محلي

### 🛠️ ماذا يفعل السكريبت:
1. ينظف المشروع السابق
2. يمسح Metro cache
3. يبني JavaScript bundle
4. يبني APK موقع (signed)
5. يعطيك المسار النهائي

### ⏱️ الوقت المتوقع:
- **أول مرة:** 30-45 ثانية
- **المرات التالية:** 15-30 ثانية

## 🔴 إذا واجهت مشاكل

### المشكلة 1: SDK غير موجود
```bash
# الحل: تأكد من تعيين ANDROID_HOME
export ANDROID_HOME=~/android-sdk
```

### المشكلة 2: السكريبت لا يعمل
```bash
# الحل: اعطه صلاحيات التنفيذ
chmod +x /workspace/FakturaVakt/build-apk.sh
```

### المشكلة 3: خطأ في Gradle
```bash
# الحل: نظف وأعد البناء
cd /workspace/FakturaVakt/android
./gradlew clean
cd ..
./build-apk.sh
```

## 🎯 الخطوات بالتفصيل الممل

### 1. تأكد أنك في المكان الصحيح
```bash
pwd
# يجب أن يظهر: /workspace أو أي مجلد آخر
```

### 2. اذهب للمشروع
```bash
cd /workspace/FakturaVakt
```

### 3. عيّن Android SDK
```bash
export ANDROID_HOME=~/android-sdk
```

### 4. شغّل السكريبت
```bash
./build-apk.sh
```

### 5. انتظر حتى ترى
```
======================================
SUCCESS!
======================================

APK Location:
  android/app/build/outputs/apk/release/app-release.apk
```

### 6. APK جاهز في
```bash
ls -la /workspace/FakturaVakt/android/app/build/outputs/apk/release/app-release.apk
```

## 📝 ملاحظات نهائية

- **هذه الطريقة الدقيقة** التي أستخدمها
- **لا تحتاج GitHub Actions** أو دفع مال
- **السكريبت ضروري** للسرعة والسهولة
- **احفظ هذا الملف** - هو الدليل الوحيد الصحيح

---

آخر تحديث: 2024-12-19
الطريقة مُجربة ومضمونة 100%