# 🚀 دليل الحصول على رابط تحميل APK (GitHub Release)

هذا الدليل يشرح كيفية تحديث التطبيق والحصول على رابط تحميل مباشر من GitHub (مثل: `https://github.com/codecsverige/Vakt/releases/tag/v0.1.0`).

## ⚠️ ملاحظة مهمة
الرابط لا يتم إنشاؤه من البناء المحلي (Local Build). يجب رفع الكود إلى GitHub ليقوم هو بالبناء والنشر (CI/CD).

---

## 📋 الخطوات بالتفصيل

### 1. تحديث رقم الإصدار
قم بتعديل رقم الإصدار في ملف `package.json`.
```json
{
  "version": "0.0.9",  <-- غير هذا الرقم
  ...
}
```

### 2. حفظ التغييرات (Git Commit)
```bash
git add .
git commit -m "feat: وصف التحديث الجديد v0.0.9"
```

### 3. الرفع إلى GitHub (Trigger Build)
هذه الخطوة هي التي تشغل GitHub Actions لبناء التطبيق وإنشاء الرابط.

```bash
# الدفع إلى الفرع الرئيسي أو أي فرع يبدأ بـ cursor/
git push origin <branch-name>
```

### 4. الحصول على الرابط
بعد الانتهاء (يستغرق حوالي 5-10 دقائق)، سيظهر الرابط تلقائياً بالصيغة التالية:

**الرابط العام للإصدار:**
```
https://github.com/codecsverige/Vakt/releases/tag/v<رقم_الإصدار>
```
مثال: `https://github.com/codecsverige/Vakt/releases/tag/v0.0.9`

**رابط تحميل APK المباشر:**
```
https://github.com/codecsverige/Vakt/releases/download/v<رقم_الإصدار>/FakturaVakt-v<رقم_الإصدار>.apk
```

---

## 🤖 ملخص للأوامر (لنسخ واللصق)

```bash
# 1. تأكد من أنك في مجلد المشروع
cd /workspace/FakturaVakt

# 2. (اختياري) تأكد من أنك على فرع مقبول
git branch --show-current

# 3. قم بالتعديلات وتحديث النسخة ثم نفذ:
git add .
git commit -m "release: new version"
git push origin $(git branch --show-current)
```
